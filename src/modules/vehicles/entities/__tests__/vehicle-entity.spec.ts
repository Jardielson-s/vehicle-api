import { VehicleEntity } from '../vehicle.entity';

describe(VehicleEntity.name, () => {
  test('should be define entity', () => {
    expect(VehicleEntity).toBeDefined();
  });

  test('should have at least one field', () => {
    expect(VehicleEntity).toBeDefined();
  });
});
